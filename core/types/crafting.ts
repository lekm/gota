/**
 * Represents the ingredients needed for a recipe (e.g., for a Gravy).
 * Keys are the baseId of the ingredient, values are the required quantity.
 */
export type GravyRecipe = Record<string, number>; 